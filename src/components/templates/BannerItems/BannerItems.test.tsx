import React from 'react'
import { render } from '@testing-library/react'
import BannerItems from './BannerItems'

describe('BannerItems Component', () => {
  test('BannerItems renders', () => {
    render(<BannerItems />)
  })
})
